<html>
<body>
    <h1>Hey, <%= name %></h1>
    <form name="form1" method="POST" >
    <table>%>
        <% var i=0;
        for(i=0;i<5;i++){ %>
        <tr>
        <td><%= courses[i] %></td>
        <td><%= nofstu[i]%></td>

        <%if (nofstu[i]<5){%>
        <td>inactive</td>
        <%}else{%>
        <td>active</td>
        <%}%>

        <td><button type="submit" name="name1" value="<%= i %>"  formaction="/course?name=<%= name %>&password=<%= password %>">register</button></td>
        <% } %>
      
    </table>
    </form>   
    <form method="POST">
            <button type="submit" formaction="/student?name=<%= name %>&password=<%= password %>">Your profile</button>
    </form>
   
</body>
</html>