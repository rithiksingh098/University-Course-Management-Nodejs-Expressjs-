<html>
    <h1> Hey,    <%= name%></h1>
    <h2> This is the list of your registered courses: <h2>
    <body>
        <% var i=0; for(i=0;i<courses.length;i++){ %>
        <p><%= i+1%> . <%= courses[i]%> <%if(nofstu[i]>2){%>-active <%}else{ %>-inactive<%}%> </p>
        <% } %>
    </body>
    <form method="POST">
            <button type='submit' formaction="<%= courseurl %>" >Get to register more courses</button>
    </form>
    <form>
        <button type='submit' formaction="/login" >Logout!</button>
    </form>
</html>